{
  "name": "backend-scaffold",
  "version": "0.1.0",
  "description": "Express JSON API with Supabase Postgres persistence and Supabase Auth (parent/kid roles)",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "test": "jest"
  },
  "jest": {
    "testEnvironment": "node",
    "roots": [
      "<rootDir>/test"
    ],
    "setupFiles": [
      "<rootDir>/test/setup.env.js"
    ]
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.4",
    "express": "^4.19.2"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "supertest": "^7.0.0"
  },
  "jest": {
    "roots": [
      "<rootDir>/test"
    ]
  }
}
