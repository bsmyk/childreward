{
  "name": "backend-scaffold",
  "version": "0.1.0",
  "description": "Minimal Express JSON API scaffold with file-based JSON persistence",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "test": "jest"
  },
  "jest": {
    "testEnvironment": "node",
    "roots": [
      "<rootDir>/test"
    ]
  },
  "dependencies": {
    "express": "^4.19.2"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "supertest": "^7.0.0"
  },
  "jest": {
    "roots": [
      "<rootDir>/test"
    ]
  }
}
